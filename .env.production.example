# Production Environment Configuration for Tessra
# Copy this file to .env.production and configure all values

# ============================================================================
# CRITICAL: Security Configuration - MUST BE CHANGED
# ============================================================================

# Admin Authentication Token
# MUST be a strong, random value (at least 32 characters)
# Generate with: openssl rand -base64 32
ADMIN_TOKEN=CHANGE_ME_TO_STRONG_RANDOM_VALUE_32_CHARS_MIN

# PostgreSQL Database Password
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_RANDOM_PASSWORD

# Redis Password
# Generate with: openssl rand -base64 32
REDIS_PASSWORD=CHANGE_ME_TO_STRONG_RANDOM_PASSWORD

# Admin Email for Let's Encrypt notifications
ADMIN_EMAIL=admin@example.com

# ============================================================================
# Domain Configuration
# ============================================================================

# Your domain name (e.g., tessra.example.com)
DOMAIN=your-domain.com

# Public URL of your application (with https://)
PUBLIC_URL=https://your-domain.com

# ============================================================================
# Database Configuration
# ============================================================================

POSTGRES_USER=postgres
POSTGRES_DB=tessra

# ============================================================================
# Application Configuration
# ============================================================================

APP_NAME=Tessra

# ============================================================================
# Storage Configuration
# ============================================================================

# Storage driver: "local" or "s3"
STORAGE_DRIVER=local

# S3 Configuration (only if STORAGE_DRIVER=s3)
# S3_ENDPOINT=https://s3.us-east-1.amazonaws.com
# S3_REGION=us-east-1
# S3_BUCKET=your-bucket-name
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key

# ============================================================================
# Access Control
# ============================================================================

# Default visibility for uploads: "public" or "private"
DEFAULT_VISIBILITY=private

# ============================================================================
# OCR Configuration
# ============================================================================

# Enable OCR processing: "true" or "false"
OCR_ENABLED=false

# OCR language code (e.g., eng, spa, fra, deu)
OCR_LANG=eng

# ============================================================================
# Performance Configuration
# ============================================================================

# Number of concurrent jobs for processing
JOBS_CONCURRENCY=5
